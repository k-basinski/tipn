---
title: "Zadanie 3"
output: html_document
---

Stwórz nowy _notebook_. Zapisz go w folderze `zad3` pod nazwą `zad3.ipynb`. Będzie on liczył twoją średnią ze wszystkich przedmiotów w tym semestrze studiów.

W pierwszej komórce (markdown) umieść nagłówek i krótki opis tego, co twoje dzieło będzie robić.

W następnej komórce utwórz osobne zmienne dla wszystkich przedmiotów, jakie masz w tym semestrze. W każdej ze zmiennych przechowywać będziesz ocenę. Pamiętaj, aby nadać zmiennym poprawne nazwy! Ma to wyglądać jakoś tak:

```python
metodologia = 5.0
ogolna = 4.5
wf = 3.0
# i tak dalej...
```

W następnej komórce utwórz zmienną, która będzie sumą wszystkich ocen. Nazwij ją `suma`. W kolejnej linijce podziel sumę przez ilość przedmiotów i umieść wynik w zmiennej `srednia`.

Na koniec wydrukuj za pomocą (jednej!) funkcji `print()` napis:

`Moja średnia w pierwszym semestrze studiów to XXX`

...gdzie XXX to oczywiście wynik działania twojego programu.
